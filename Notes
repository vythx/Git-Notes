# Initialize Git
1. Create Your Project's Folder
2. Open Git
3. Open Your Folder @cd <Path To Your Folder>
git init

# Check Branches
git branch

# Create A New Branch
git checkout <New Branch Name>

# Switch To A Branch
git checkout <Branch Name>

# Prep File(s) For Commit or Tracking
git add <File Name> @Single File
git add -A @Multiple Files

# Commit File(s) or Save Changes
git commit <File Name> -m <"Your Comment"> @Single File
git commit -m <"Your Comment"> @Multiple Files

# View commits
git log @View Commits

# Undo Commits
git checkout <hash>

# Push Files To GitHub
1. Create A GitHub Account or Open Your Existing Acount
2. Create A Repository

git remote add origin https://github.com/<Your User Name>/<Your Repository Name>.git
git branch -M main
git push -u origin main

# Pull files from Github
git push -u origin main